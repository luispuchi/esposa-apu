<div class="row">
  <div class="col-md offset-md-1">
    <%= form_with(model: baby, local: true) do |form| %>
      <% if baby.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(baby.errors.count, "error") %> prohibited this baby from being saved:</h2>
          <ul>
            <% baby.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <div class="field">
        <%= form.label :name, "Nombre" %>
        <%= form.text_field :name %>
      </div>
      <div class="field">
        <%= form.label :gener, "Genero" %>
        <%= form.select :gener, Baby.geners.keys %>
      </div>
      <div class="actions">
        <%= form.submit "Guardar"%>
      </div>
    <% end %>
  </div>
</div>